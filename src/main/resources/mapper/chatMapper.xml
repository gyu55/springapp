<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.app.springapp.mapper.ChatMapper">
    <select id="selectAll" parameterType="Long" resultType="ChatDTO">
        SELECT
            TBC.ID, TBC.CHAT_CONTENT, TBC.CHAT_CREATE_AT, TBC.CHAT_TYPE, TBC.USER_ID, TBC.CHAT_ROOM_ID,
            TBU.USER_NICKNAME, TBU.USER_PROFILE
        FROM TBL_CHAT TBC
                 JOIN TBL_USER TBU ON TBC.USER_ID = TBU.ID
        WHERE TBC.CHAT_ROOM_ID = #{chatRoomId}
        ORDER BY TBC.ID DESC
    </select>

</mapper>