<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.app.springapp.mapper.ChatMemberMapper">
    <insert id="insert" parameterType="ChatMemberVO">
        INSERT INTO TBL_CHAT_MEMBER (ID, USER_ID, CHAT_ROOM_ID)
        VALUES (SEQ_CHAT_MEMBER.NEXTVAL, #{userId}, #{chatRoomId})
    </insert>

</mapper>